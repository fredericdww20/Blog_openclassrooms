{%  extends 'base.html.twig' %}

{% block body %}
<div class=" flex justify-center mt-5">
    <div class="grid grid-cols-3 gap-4 place-items-stretch h-15">
    <div class="max-w-sm rounded overflow-hidden ">
        <div class="px-6 py-4">
            <div class="font-bold text-xl mb-2"> {{ session.lastname }} {{ session.firstname }}</div>

        </div>
        <div class="px-6 pt-4 pb-2">

        </div>
    </div>
    </div>
</div>
{% endblock %}
